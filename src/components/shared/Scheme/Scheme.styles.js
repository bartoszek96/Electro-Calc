import styled from 'styled-components';

export const StyledScheme = styled.figure`
  color: white;
  text-align: center;
  margin-bottom: 35px;
  padding: 10px;
  background: rgba(0, 0, 0, 0.5);
  border-radius: 10px;

  img {
    height: 200px;
    width: 200px;
    margin: 5px;
  }
`;

export default StyledScheme;
